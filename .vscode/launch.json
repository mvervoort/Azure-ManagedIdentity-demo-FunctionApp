{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Setup.ps1",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/Setup.ps1",
            "cwd": "${cwd}"
        },
        {
            "name": "Attach to .NET Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}